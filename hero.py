class pet:
    def __init__(self, name, hungry,):
        self.hungry = hungry
        self.name=name
    def newP():
        hungry=100
        name=input("name")
        return pet(name,hungry)
    def starve(self):
        for i in range(100):
            print(self.hungry)
            self.hungry = self.hungry -1

hero = pet.newP()
john.starve()
